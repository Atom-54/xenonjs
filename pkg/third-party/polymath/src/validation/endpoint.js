//import { schemas } from "@polymath-ai/types";
export const validateResponse = (result) => {
    return result;
    //return schemas.packedLibraryData.parse(result);
};
export const validateEndpointArgs = (args) => {
    return args;
    //return schemas.endpointArgs.parse(args);
};
